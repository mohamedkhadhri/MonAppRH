<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.myapplicationrh">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplicationRH"
        tools:targetApi="31">

        <!-- Activités déclarées -->

        <activity android:name=".MainActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".LoginActivity" android:exported="true" />
        <activity android:name=".AdminActivity" android:exported="true" />
        <activity android:name=".AdminDemandeCongeActivity" android:exported="true" />
        <activity android:name=".PlanningActivity" android:exported="true" />
        <activity android:name=".PresenceAbsenceActivity" android:exported="true" />
        <activity android:name=".UserActivity" android:exported="true" />
        <activity android:name=".RegisterActivity" android:exported="true" />
        <activity android:name=".UserListActivity" android:exported="true" />
        <activity android:name=".CongeActivity" android:exported="true" />
        <activity android:name=".DetailPresenceActivity" android:exported="true" />
        <activity android:name=".EditUserActivity" android:exported="true" />



    </application>

</manifest>
